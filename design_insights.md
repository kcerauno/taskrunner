# runbook 設計考察 — 動機への立ち返りと追加改善提案

improvement_proposals.md の12案のレビュー(improvement_proposals_review.md)の後、「そもそもの動機に立ち返る」視点から得られた追加提案と、問題の本質に関する考察をまとめる。

---

# 第1部: 視点の転換と追加改善提案

## Excel手順書は「コマンド実行機」ではなかった

既存の12案は、いずれも「実行エンジンとしての runbook を強化する」方向を向いている。しかし元の Excel 手順書が現場で担っていた役割を分解すると、コマンド実行は一部にすぎない。実際の Excel 手順書には、およそ次のものが載っていた。

1. **コマンド以外の作業指示** — 「VPNに接続する」「監視を一時停止したことを画面で目視確認」「顧客に開始連絡を入れる」
2. **失敗したときにどうするかの知識** — 備考欄の「これが失敗したら○○を確認、ダメなら△△さんにエスカレーション」
3. **切り戻し手順** — 手順書の後半に必ずある「戻し」のセクション
4. **誰がやったかの記録** — 作業者/確認者の欄

現状の runbook はこのうち「コマンド実行と判定」だけを引き取っており、上の4つはツールの外に残っている。ここが埋まらないと、現場は「自動化できる部分は runbook、それ以外は Excel」という**二重管理**になり、置き換えという動機そのものが達成できない。

## 提案A: 手動確認ステップ(最重要)

現状、RB-CMD のないステップはパースエラーになる(parser.py)。つまり「目視確認」「手作業」「関係者連絡」は手順書に書けない。

RB-CMD なしのステップを「手動ステップ」として許容し、RB-DESCRIPTION を表示して作業者の完了確認(y で続行)を待ち、確認時刻をログに残す。これだけで**手順書全体を1つの .md に統合でき、Excel を完全に廃止できる**。新記法は不要(既存セクションの省略を許すだけ)で、シンプル志向とも整合する。12案のどれよりも「動機の達成」への寄与が大きい。

## 提案B: 失敗時ガイダンス(RB-ONFAIL)

「失敗即中断」は確定仕様だが、中断した*後*の作業者への支援は空白である。深夜の障害対応で一番価値があるのは、手順書作成者が知っていた「これが失敗したら次はこう動く」という知識。

ステップに任意の RB-ONFAIL セクション(人間向けの文章)を書けるようにし、NG で中断した瞬間にそれを表示する。自動修復(却下した提案7)と違い、判断も実行も人間のまま知識だけを届けるので、即中断の思想と矛盾しない。

## 提案C: 切り戻しセクションの一級市民化

Excel 手順書の「切り戻し手順」に相当する仕組みがない。同じ .md 内に切り戻し用のステップ群を宣言でき、通常実行では走らず、中断時に「切り戻しは `runbook run xxx.md --rollback` を実行」と案内が出る形。**自動では絶対に走らせない(案内するだけ)**のがポイントで、ここでも人間が主導権を持つ。

## 提案D: 作業者・確認者の記録

証跡として見ると、現状のログには「いつ・何を・結果どうだったか」はあっても**「誰が」がない**。Excel の作業者/確認者欄はダブルチェック文化の器だった。実行開始時に作業者名(と任意で確認者名)を入力させ result.json と run.log に記録するだけで、証跡の完全性が大きく上がる。実装は極小。

## 提案E: 実行前サマリー確認

逆説的だが、Excel は「開いて読んでから作業する」ため遅い分だけ慎重さが担保されていた。CLI は一打で走るため、**手順書やインベントリの取り違えが起きやすくなった**とも言える。実行開始前にタイトル・ステップ数・使用インベントリ・変数の一覧を表示して確認を挟むゲートは、インベントリのデフォルト禁止と同じ「事故防止を仕様で担保する」思想の延長。

## 優先順位とメタな意見

**A → D → B → E → C** を推奨する。

このツールの最大のリスクは機能不足ではなく、**チームメンバーが手順書を Markdown で書いてくれるか**という採用の壁である。実行エンジンを豪華にする改善(元の12案の多く)より、「Excel でできていたことが全部できる」状態を最短で作る改善(A〜D)の方が、動機に対して効率が良い。逆に言えば、A〜D が揃うまでは新しい実行系機能を足さず表面積を小さく保つこと自体が、一つの改善方針である。

---

# 第2部: 問題の本質に関する考察

## 1. 確定要件はすべて、たった一つの原則の現れである

これまでの設計判断 — 失敗即中断、自動リトライなし、AI修復の却下、インベントリのデフォルト禁止、fail-loud、新記法の拒否 — は一見バラバラの好みに見えるが、全部が同じ一つの原則から導出できる。

> **「機械は実行と判定を担うが、継続の決定は常に人間が担う」**

これはこのツールの憲法である。この原則を明示すると、runbook が自動化スペクトラム上のどこに立っているかが正確に言える。完全手作業(Excel)と完全自動化(playbook一発実行やCIパイプライン)の中間だが、単に「中途半端な自動化」なのではなく、**信頼の配分を意図的に選んだ点**である。チームには既に Ansible があり、全手順を一つの playbook にすることも技術的には可能なのに、そうしない。playbook は途中の状態を隠蔽し、異常時に人間のチェックポイントを挟めないからだ。runbook の存在理由は「実行の自動化」ではなく「異常時に必ず人間へ主導権を返す自動化」であり、劣化版 Ansible ではなく別の設計思想である。

実利: この原則を一文で README に書いておけば、今後どんな機能提案が来ても数秒で判定できる。提案7(AI修復)と11(RaaS)は原則違反として即却下、提案A〜Dは原則適合として通る。個別判断の集積が、検証可能な設計ルールに変わる。

## 2. 核心は「実行の自動化」ではなく「判断の形式化」である

Excel手順書の「想定結果」欄は、典型的には「正常に完了すること」「エラーが出ないこと」だった。これは判断基準ではなく、**判断の先送り**である。実行者が現場で「これは正常か?」を自力で判定することを暗黙に要求していた。

criteria はここを変える。`rc == 0 and out("active") and not out("error")` と書くには、作成者が設計時点で「正常とは正確に何か」を答えなければならない。**コマンドを書くのは簡単で、真偽条件を書くのが難しい** — この難しさこそが価値である。ツールが本当に自動化しているのは打鍵ではなく、平時の専門家の判断を深夜の非専門家の手元に届けることである。

ここから一つ導かれる。「Excel移行は手動」という決定は、思っていた以上に正しかった。移行作業の本体は Markdown への書き写しではなく、**曖昧だった想定結果を真偽条件に翻訳する暗黙知の発掘作業**だからだ。変換ツールを作っていたら、曖昧さがそのまま機械化されていた。したがって、チーム展開で本当に効く支援は記法の教育ではなく「良い criteria の書き方パターン集」である。移行の律速段階はそこにある。

## 3. 実行可能なドキュメントとは「反証可能なドキュメント」である

手順書の宿命は腐ることだ。システムは変わり、手順書は変わらず、その乖離は**最悪のタイミング(本番作業中)に発覚**する。Excel手順書は原理的に「失敗できない」ドキュメントだった — 間違っていても、実行者が現場で気付いて黙って読み替えるだけで、文書自体は訂正されない。

runbook はこの性質を根本から変える。criteria 付きの手順書は、実行のたびに全ステップが検証されるので、**手順書の腐敗が実行のたびに検知される**。ドキュメントが反証可能になった。これは documentation-as-code の一般論より強い性質で、このツールの一番静かで一番深い価値である。

ただし系として限界も見える。この検証は実行頻度に比例してしか働かない。月次作業の手順書は生き続けるが、**滅多に使わない障害対応手順書ほど腐ったまま放置され、しかも一番重要な瞬間に使われる**。却下した提案12(カオス検証)には、実はこの一粒の真実が含まれていた。障害注入は不要だが、「稀にしか走らない手順書をステージングで定期的に素振り実行する」運用(手順書のCI)は、この構造的弱点への合理的な対処である。これはツール機能というより運用規律の話。

## 4. 自動化のパラドックス — 速くなった分、失われるものがある

ヒューマンファクター研究に Bainbridge の「自動化の皮肉」(1983)という古典的知見がある。定型作業を自動化すると、人間の役割は「自動化が対処できない例外への対応」だけになるが、**そのとき人間は普段の実践機会を奪われているため、例外対応の能力と状況認識が最も落ちている**、というもの。

runbook に当てはめると: Excel時代の作業者は14ステップを手で打鍵していたので、ステップ15で異常が起きたとき、システムの現在状態が頭に入っていた。runbook の作業者は14ステップを眺めていただけなので、中断の瞬間の状況把握が浅い可能性がある。**速くなったこと自体が、失敗時の対応力を削っている**かもしれない。

興味深いのは、これまで直感で積み上げてきた仕様 — 簡素な行ベース出力、ステップごとのタイムスタンプ表示、ターミナル出力をそのままログとして残す運用 — が、期せずしてこのパラドックスへの対抗策になっていることだ。実行過程を可視で追える設計は、作業者のメンタルモデルを維持する装置として機能している。今後の設計判断でも**「これは作業者の状況認識を保つか、奪うか」**という評価軸を持つと、この直感を意識的な基準に昇格できる。提案B(失敗時ガイダンス)が重要なのも、認識が最も浅い瞬間に文脈を注入する仕組みだからである。

## 5. 証跡の因果が逆転している

Excel運用では、作業が先にあり、証跡は後から人手で組み立てるものだった(結果の転記、スクリーンショットの貼り付け)。つまり証跡は作業の**写し**であり、写しである以上、転記ミスも粉飾も物理的に可能だった。runbook では実行そのものが証跡を生成するので、**作業と証跡が同一物**になる。監査的観点では、これは「改ざん防止機能を足す」(提案10)よりずっと手前の、構造的な信頼性向上である。証跡署名を検討する前に、この構造自体が既に Excel より強いことは認識しておく価値がある。

## 6. 手順化できないものの存在を、設計として認める

「ダッシュボードのグラフが普段どおりの形か」「この顧客影響なら続行してよいか」のような判断は、事前に真偽条件へ形式化できない。これは実装の不足ではなく、暗黙知(Polanyi)の性質上の限界である。成熟した設計は、この境界を否定するのではなく、**境界での引き継ぎを丁寧に設計する** — 形式化できる8割は criteria で機械判定し、できない2割は手動確認ステップ(提案A)として人間の判断を明示的に手順へ組み込む。「全部自動判定できるはず」という誘惑に抗うことも設計である。

同じ論理で、**移行しない判断**も規律の一部である。一度きりの移設作業のような手順は、criteria を書くコストが実行回数で回収できない。「何を runbook 化しないか」の基準(目安: 2回以上実行される見込みがあるか)をチームで持つことが、ツールの信頼を守る。

---

# 結論

このプロジェクトの本当の成果物は CLI ではなく、**「機械が実行と判定を、人間が決定を担う」という運用規律そのもの**であり、ツールはその規律を強制するための装置である。

だとすれば、最もレバレッジの高い「改善」はコードではなく、この設計原則と運用規律を1枚のドキュメントにして、チームの共有物にすることかもしれない。

- 設計原則(機械は実行と判定、人間は決定)
- criteria の書き方パターン集
- 稀に使う手順書の定期素振り実行(手順書のCI)
- 何を runbook 化しないかの基準
