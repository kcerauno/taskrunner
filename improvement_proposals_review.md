# improvement_proposals.md レビュー結果

確定済みの設計要件(ローカル実行のみ・失敗即中断・新記法を増やさないシンプル志向・テキストログ運用)と照らして、12案を評価した。

結論: **採用推奨は 1・2・4 の3件**。3 と 8 は形を変えれば有用。**7・11・12 は確定要件と正面から矛盾するため不採用を推奨**。

## 評価サマリー

| # | 提案 | 評価 |
|---|------|------|
| 1 | 判定の詳細診断 | ◎ 採用推奨(最有力) |
| 2 | ドライラン/静的検証 | ◎ 採用推奨(`check` 拡張として) |
| 3 | HTMLレポート | ○ 単一ファイル静的HTML限定なら可 |
| 4 | シークレットマスキング | ◎ 採用推奨(方式は要変更) |
| 5 | ステップ間データ引き継ぎ | △ 保留(share_env で概ね代替可) |
| 6 | SQLite履歴管理 | △ 見送り(現行運用と逆方向) |
| 7 | AI自律修復 | ✗ 不採用(失敗即中断の仕様と矛盾) |
| 8 | タイムトラベルデバッガー | ○ `--start-from` に置き換えを提案 |
| 9 | record 自動生成 | △ 見送り(手動移行の方針と不整合) |
| 10 | 監査署名ロック | △ 見送り(SHA-256 マニフェストで代替) |
| 11 | Runbook-as-a-Service | ✗ 不採用(ローカル実行のみの要件と矛盾) |
| 12 | カオス検証 | ✗ 不採用(前提の retry 機構が存在しない) |

---

## 採用推奨

### 1. 判定の詳細診断

最有力。criteria.py は既に AST ホワイトリスト評価器なので、部分式ごとの評価結果を拾う実装は小さく済む。NG 時に「どの条件で落ちたか」が即読めるのは、このツールの核心価値(判定の明確さ)を直接高める。fail-loud 志向とも完全に一致する。

### 2. ドライラン/静的検証

推奨。ただし新コマンドではなく、**既存の `runbook check` の拡張**として実装すべき(見出し順序警告など既に静的検証の受け皿がある)。

- 変数未定義チェック
- インベントリ / playbook のパス存在チェック
- criteria 構文の一括検証
- 展開後コマンドのプレビュー

いずれも「本番の途中で初めて気付く」事故を防ぐ実益がある。インベントリ明示を仕様で強制した動機(指定漏れ事故の防止)と同じ思想。

### 4. シークレットマスキング

課題認識は正しく、ログをテキスト保存して回覧する運用なら必須級。ただし `SECRET_` プレフィックス方式は再考を勧める。これは「大文字限定記法」を却下したのと同種の**暗黙の命名規則**で、知らない人には効かない・気付けない仕組み。

代わりに vars 定義側で明示的に `secrets: [DB_PASS]` のように宣言する方式が、既存の設計思想(明示・fail-loud)に合う。Ansible Vault 連携は第2段階で十分。

---

## 形を変えれば有用

### 3. HTMLレポート

「Excel手順書の置き換え」という出自を考えると、非エンジニアへの証跡報告ニーズは本物。ただし外部依存なしの**単一ファイル静的HTML**(result.json を埋め込み)に限定すべき。サーバーや CDN 依存が入るとローカル完結の原則が崩れる。優先度は中。

### 8. タイムトラベルデバッガー

このままでは不採用を勧める。VM スナップショットや Git 自動コミットで巻き戻せるのはローカル状態だけで、ansible で変更したリモートホストの状態は戻せないため、「ロールバック」という約束が守れない。

ただし課題(終盤で失敗すると最初からやり直し)は実在するので、**`--start-from N`(指定ステップからの再開)** という軽量機能に置き換えることを提案する。share_env の shared_env.sh が残っていれば変数状態の復元もほぼ賄える。実装は小さく、効果は提案の大半をカバーする。

---

## 確定要件と矛盾するため不採用推奨

### 7. AI自律修復

「ステップ失敗時は常に即中断(自動継続なし)」という確定仕様と正面衝突する。加えて、LLM が提案した `sudo apt install` を本番サーバーで対話承認して実行する仕組みは、監査性を重視する現場(提案10で自ら金融・監査を挙げている)では受け入れられない。

### 11. Runbook-as-a-Service

「実行先はローカル bash のみ、SSH実行機能は不要」という確定要件と矛盾する。Slack/Jira 連携・認可システムはツールの性格を根本から変える別プロダクトの話。

### 12. カオス検証

提案文中の「`retry` 機構が正常に機能するか」という記述自体が誤りで、このツールに retry は存在しない(失敗即中断が仕様)。検証対象が存在しない以上、成立しない。

---

## 保留・見送り推奨

### 5. ステップ間データ引き継ぎ(register)

bash ステップでは `share_env: true` + `export CONTAINER_ID=$(...)` で既に実現できる。regex による register 記法は「新記法を増やさない」方針に反するので、ansible ステップの出力を後続で使いたい具体的ニーズが出てから検討で十分。

### 6. SQLite

「Git でテキスト差分管理」「実行ターミナル出力をログとして保存」という現行運用と逆方向。個人〜チーム規模なら result.json + jq で足りる。提案自体が「予備候補」としている通り、見送りが妥当。

### 9. record(操作記録からの自動生成)

「Excel移行は手動でMarkdown化」と決めた経緯と整合しない。AI による criteria 自動合成は品質保証が難しく、間違った判定基準が量産されるリスクの方が大きい。代わりに `runbook new` でテンプレート雛形を出す程度が現実的。

### 10. 監査署名ロック

WORM ストレージや PDF/A、タイムスタンプ局は明らかに過剰装備。改ざん検知だけなら、実行終了時に**ログ一式の SHA-256 マニフェストを生成**する軽量案で目的の8割を達成できる。必要になったらそこから始めるべき。

---

## 細かい指摘

- 提案5のコード例はフェンスが閉じておらず Markdown が壊れている(improvement_proposals.md 67〜71行目)。
- 提案10に「暗号暗号化」という誤記がある(improvement_proposals.md 126行目)。

## 着手順の提案

**1 → 2(check拡張) → 4(明示宣言方式) → 8の軽量版(--start-from)**

この4つはいずれも既存アーキテクチャの延長で実装でき、ツールの性格を変えない。
