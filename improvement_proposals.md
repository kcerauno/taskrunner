# runbook CLI 今後の改善提案

本ツールの実用性、信頼性、およびユーザー体験をさらに高めるため、以下の12の観点から改善提案を行います。

---

## 1. 正常性基準判定の詳細診断機能 (Conjunction Diagnosis)
### 現状の課題
正常性基準（`step.criteria`）に複数の条件（`rc == 0 and out("active") and not out("error")` など）が指定されていて判定が `NG` になった際、どの部分条件が原因で失敗したのかがコンソール表示だけでは判読しにくい。

### 改善案
基準式の評価時に抽象構文木 (AST) を解析または部分評価し、どの条件式が `False` になったのかを明示的にブレイクダウン表示します。

```text
  ├ 詳細: 正常性基準を満たしませんでした
  │   診断結果:
  │     [✓] rc == 0 (実績: 0)
  │     [✗] out("active") (stdout 内に "active" が見つかりませんでした)
  │     [✓] not out("error")
```

---

## 2. ドライラン / 静的バリデーション判定 (`--dry-run`)
### 現状の課題
実行を開始するまで、Jinja2の変数展開エラーやAnsibleのインベントリ指定ミスなどに気付けない。本番環境でステップの途中で文法エラーによる中断が起きるリスクがある。

### 改善案
実際にコマンドを起動せずに、以下の検証を行うドライランモードを実装します。
- 参照される変数（`vars`）がすべて定義されているかの定義チェック
- Ansible ad-hoc や playbook の指定ファイル・インベントリが存在するかのパスチェック
- `criteria` 式が文法的に有効かどうかの検証 (`criteria.validate` の事前一括稼働)
- 代入後のコマンドライン文字列のプレビュー出力

---

## 3. 実行ログからのインタラクティブHTMLレポート出力 (`runbook report`)
### 現状の課題
実行結果（Logs）はローカルのテキストファイル（`run.log`, `result.json`）として保存されるが、別チームや非エンジニアのステークホルダーに証跡として報告する際に、そのまま貼るには可読性が低い。

### 改善案
`logs/` 配下の `result.json` を元に、動的なHTMLレポートファイルを生成する機能を追加します。
- ステップごとのアコーディオン開閉（ログ詳細表示）
- ホスト別結果マトリックスのグリッド表示と検索フィルタ
- 実行環境（変数リスト、時刻など）のメタデータサマリーカード表示

---

## 4. シークレット保護と環境変数注入 (Secrets Masking)
### 現状の課題
パスワードやAPIトークンなどの機密情報が手順書内の変数（`vars`）や実行時引数（`--var`）に指定された場合、そのまま stdout やログファイル (`run.log`, `result.json`) に平文で記録・出力されてしまう危険性がある。

### 改善案
- 暗号化された値のサポート（Ansible Vault との連携や、特定プレフィックス `SECRET_` の自動マスク化）。
- コンソールおよびログファイル出力時に、該当する値を `********` に置換するマスキング機能の導入。

---

## 5. ステップ間のデータ引き継ぎと動的変数設定
### 現状の課題
ステップ1で実行したコマンドの出力結果（例：起動したコンテナのIDや自動生成されたパスワード）を、ステップ2以降のコマンドに動的に引き継ぐ標準的な方法がない。

### 改善案
ステップ実行後の標準出力を正規表現などでパースし、後続のステップで利用可能な環境変数として登録する機能を拡張します。
```markdown
### RB-LOCALDEF
```yaml
register:
  CONTAINER_ID: "^CID: (\\w+)"
```
上記のように記述することで、後続ステップで `{{ CONTAINER_ID }}` を自動利用可能にします。

---

## 6. SQLite3による履歴・統計ログのデータベース管理 (予備候補)
### 現状の課題
ログがJSONおよび各ステップのテキストファイルとして散らばって保存されており、過去の実行履歴に対する長期的統計（ステップごとの平均実行時間の推移、失敗が多いホストの特定など）を検索・分析する際に、複雑なコマンドライン集計（Grepやjq等）が必要となる。

### 改善案
実行ログ（ホスト別結果・実行結果）を SQLite3 データベースファイルに格納するオプションを追加します。
- 一つの `.db` データベースファイルで全履歴を一元管理可能にする。
- SQL クエリにより「過去1ヶ月で最も失敗している特定ホスト」などを簡単に分析可能にする。
- Webダッシュボードや社内の監視ポータルとORMを介して即座につなぎ込めるようにする。
- （留意点）Git でのログのテキスト差分 (Diff) 管理やバージョン制御を行いたいユースケースを考慮し、既存の JSON/テキストファイル出力モードとの排他（または併用）設定ができる仕様とします。

---

## 7. AI支援型 自律修復・回復モード (`--auto-repair`)
### 現状の課題
手順書の途中で予期せぬパッケージ未導入や権限エラー（Permission Denied）、パス指定ミスなどが起きると、人間が手動で原因を調査してターミナルに戻り、修復作業を行った上で手順書を再実行する必要がある。

### 改善案
手順書実行が正常性基準（`criteria`）で失敗した際、エラー箇所のコマンドと標準エラー出力をローカルLLM等で解析させます。
- 解析後、「パッケージの不足が原因です。`sudo apt install -y ...` を実行して修復し、実行を再開しますか？ [Y/n]」のような解決策をその場で提示し、実行させます。
- 単なる受動的なスクリプト実行機から、「自己治癒するスマートエージェント」へと進化させます。

---

## 8. タイムトラベル・ステップデバッガー (`--interactive-debug`)
### 現状の課題
手順書の終盤（例：ステップ15）で失敗すると、最初からやり直すか、失敗した箇所に追いつくまでの変数を手動でセットアップし直す必要があり、確認に多大な時間がかかる。

### 改善案
各ステップを実行する前に、一時的な環境変数やローカルワークスペース（Gitの自動コミットやVMのスナップショット）の状態を自動で退避します。
- ステップが失敗した際、コンソール上で `r` (Rollback) キーを押すと、自動的に状態を直前のステップ開始時点へ巻き戻します（ロールバック）。
- そのままターミナルを立ち上げ続けながら、手順書のMarkdown（`.md`）ファイルをエディタ等で修正し、`Enter` を押すだけで、「修正後の内容でそのステップから実行を再試行」可能にします。

---

## 9. 操作手順からの手順書コード自動生成 (`runbook record`)
### 現状の課題
実行する手順書を作る際、コマンド、説明、想定結果、Jinja2の埋め込み等を含めたMarkdownをすべて設計して記述するのにエンジニアの手順書作成コストがかかる。

### 改善案
シェル上で `runbook record` コマンドを実行し、オペレータは通常通りサーバーでインフラ作業（パッケージインストール、ログ確認など）を行います。
- 途中で対話型コマンドやエディタで行った作業、およびコマンドの終了ステータスと出力を裏で追跡（レコード）します。
- 記録終了時に、AIが操作ログを推論し、ステップ構成の切り分け、各ステップのコマンド、見出し(`##`)、そして自動で判定基準(`criteria`の`rc == 0 and out(...)`)を合成し、1つの綺麗なMarkdown手順書を自動生成します。

---

## 10. 金融・監査向けの証跡自動署名ロック (Compliance Evidence Locker)
### 現状の課題
金融業界やセキュリティ監査が必要な部署（コンプライアンス管理部門）では、実行された変更コマンドや出力結果が「改ざんされていないこと」の証明責任があるが、プレーンなテキストログやJSONでは後から編集が可能なため、信頼性保証に限界がある。

### 改善案
ログの信頼性を証明するための暗号暗号化・署名機能を実装します。
- 実行ログ一式に対し、実行したオペレータの鍵（SSHキー / OIDCトークン）やタイムスタンプ局のデータを用いて暗号署名を自動付与。
- 証跡として、改ざん不可能なPDF/Aファイル、またはZIP圧縮署名アーカイブを自動合成し、WORM（書き込み一回メモリ）ストレージや監査バケットに自動パブリッシュする仕組みを提供します。

---

## 11. ヘルプデスク連携によるトラブルシューティング自動化 (Runbook-as-a-Service)
### 現状の課題
カスタマーサポートや営業部門において「特定テナントのステータス詳細確認」「一時的なキャッシュクリア」など定型的なシステム操作が必要な際、その都度SREや開発者に依頼がトスされ、対応までにリードタイムが発生する。

### 改善案
ChatOps / コミュニケーションツールのAPI（Slack, Jira, Zendesk等）と連携する「Runbook-as-a-Service」モデルを提案します。
- サポート窓口でチケットが起票されたら、事前定義された「確認用読み取り手順書」がバックグラウンドで安全に検証・実行され、結果を自動で担当トピックに書き込みます。
- オペレータは `Slack` 等の認可システムを通してのみ、あらかじめ承認された手順書を限定的な引数（例：`--var tenant_id=123`）で安全に発火させることができます。

---

## 12. カオス実験による手順書自体の堅牢性検証 (`--chaos-run`)
### 現状の課題
本番障害対応用の手順書が「平時（ネットワークに遅延がなく、CPUに余裕がある状態）」でしかテストされておらず、本番の輻輳エラー発生時に手順書のステップ（タイムアウト設定や例外ハンドリング）自体がコケる可能性がある。

### 改善案
あらかじめ手順書自体の「復旧能力（レジリエンス）」をテストする検証モードを提案します。
- サンドボックスまたはステージング上での検証時、各ステップの実行と並行して、バックグラウンドでネットワーク遅延の注入（packet loss 20%等）やCPUの高負荷をシミュレート。
- この障害シミュレーション中であっても、手順書の `retry` 機構が正常に機能するか、`criteria` が不慮の遅延によるタイムアウトエラー等を堅牢に検知できるかを検証・スコアリングします。
